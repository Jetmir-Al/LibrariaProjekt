@using LibrariaProjekt.Server.Models
@model List<Borrow>
@{
    Layout = "_Layout";
}

<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>UserId</th>
                <th>BookId</th>
                <th>BorrowDate</th>
                <th>ReturnDate</th>
                <th>Total</th>
                <th>CardholderName</th>
                <th>CardNumber</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var entry in Model)
            {
                <tr>
                    <td>@entry.Id</td>
                    <td>@entry.UserId</td>
                    <td>@entry.BookId</td>
                    <td>@entry.BorrowDate</td>
                    <td>@entry.ReturnDate</td>
                    <td>@entry.Total</td>
                    <td>@entry.CardholderName</td>
                    <td>@entry.CardNumber</td>
                    <td>
                        <a asp-controller="Borrow" asp-action="Edit" asp-route-id="@entry.Id" class="btn btn-primary col-xs-3">Edit</a>
                        <a asp-controller="Borrow" asp-action="Delete" asp-route-id="@entry.Id" class="btn btn-danger col-xs-3">Delete</a>


                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
