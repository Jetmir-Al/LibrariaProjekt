import { createContext } from "react";
            
export const ToggleAccountContext = createContext();

export const ToggleLightDarkContext = createContext();

export const ToggleBorrow = createContext();
export const ToggleBuy = createContext();
